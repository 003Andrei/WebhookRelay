services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: hook
      POSTGRES_PASSWORD: hook
      POSTGRES_DB: hookrelay
    ports: ["5432:5432"]
    volumes: [dbdata:/var/lib/postgresql/data]
volumes:
  dbdata:
